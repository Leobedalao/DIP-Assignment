[project]
name = "pix2pix-training"
version = "0.1.0"
description = "Simple Pix2Pix training project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
    "opencv-python",
    "torch",
]

[tool.uv]
package = false
